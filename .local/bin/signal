#!/bin/bash

# GDK_BACKEND=wayland exec flatpak run org.signal.Signal --enable-features=UseOzonePlatform --ozone-platform=wayland "$@" #> /dev/null 2>&1
exec flatpak run org.signal.Signal "$@" > /dev/null 2>&1
